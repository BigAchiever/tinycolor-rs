[package]
name = "tinycolor-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
regex = "1.11"
once_cell = "1.20"
serde_json = "1.0.151"

[lints.rust]
# Behavioural-equivalence bonus (+5) is scored on unsafe count.
# This makes `unsafe` a compile error, so the claim is machine-checked, not asserted.
unsafe_code = "forbid"
