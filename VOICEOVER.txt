One assertion in this suite failed against my port. Off by a single bit.

The bug wasn't in my Rust. It wasn't in the JavaScript either.

I'll come back to that.

...

This is TinyColor's own test suite, hash-pinned at the kickoff commit. Zero bytes changed.

That matters for what happens next.

...

This is that file. Two thousand one hundred and ninety-one lines, four hundred and thirty-two assertions, written for the JavaScript library.

Line five is how it loads what it's testing.

...

One require. So I put mine at that path.

...

Same file. Never edited. Now loading Rust.

...

A hundred and ten lines of adapter. None of it does colour maths.

...

Same suite. Same forty-five. But not WebAssembly this time — a native Rust binary on the other end of a pipe.

The correctness lives in the port, not the plumbing.

...

Two fair objections. First — the original is in this repo. Is the port calling it?

...

Deleted. Still forty-five. It's only the fuzzer's reference.

...

Second — that's my runner printing the number. Would it print a smaller one?

...

Break one method and it says forty, and names the test. Not a rubber stamp.

...

A fixed suite is table stakes. So — a differential fuzzer.

Random colours into the original on V eight, and into the port. Compared bit for bit. Floats match exactly, or it's a divergence.

...

Five minutes. Six hundred and eighty-nine thousand generated colours, forty-five checks on each — thirty-one million comparisons. Zero divergences.

Same on the native transport.

...

So — that failing assertion.

Relative luminance. Off by one unit in the last place — the smallest disagreement two doubles can have.

I assumed a rounding bug. It wasn't. The answer changed depending on what I built for. Native Rust gave one number. The same source, as WebAssembly, gave another.

...

Math dot pow isn't bit-identical across libm implementations. Over all two hundred and fifty-six channel values, macOS matched V eight on two hundred and seven. WebAssembly's on two hundred and twenty-five. Neither matches.

But luminance only ever sees integers, zero to two fifty-five. That's not a function — that's a lookup table.

So it's a generated table of V eight's exact bits. Bit-exact everywhere, and faster than pow.

...

TinyColor's own demo page, unmodified. Every swatch computed in Rust.

...

Same global, same API. Nothing downstream had to change.

...

Now the part most demos skip.

The port is slower. Five to ten times, per operation. And the WebAssembly build loses on startup and memory too — seventy-one milliseconds against eighteen.

...

But it isn't the colour maths. Peel the layers apart: the ported Rust runs at one point three times the original. The shipped artifact, eight.

Ninety-six percent of the gap is the bridge — one JSON boundary instead of forty-five F F I signatures. That's the only reason the unmodified suite can drive the port at all.

I'd make the same call again. I won't pretend it was free.

...

The win is runtime elimination. The native binary starts nearly six times faster, in four megabytes instead of thirty-four — carrying the same forty-five, and the same fuzz evidence.

And zero unsafe, in all three crates. Not a README claim — forbid makes it a compile error.

...

Twenty-two documented divergences. Math dot round breaks ties differently in the two languages. The string one point zero, and the number one point zero, are different colours.

And a real bug in the original — analogous with a negative count loops forever. Filed upstream.

...

Four of these are bugs in my port. A handle table that leaked until the WebAssembly heap died. A transport that hung every process using it.

A decision log that only records other people's mistakes isn't one.

...

Correct against the original's own tests, on two transports, with the regression on the card instead of buried. All of it reproduces from a clean checkout.
